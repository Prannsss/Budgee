# ================================================
# Budgee Backend Environment Variables
# ================================================

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=budgee_dev
DB_USER=postgres
DB_PASSWORD=your_password

# Alternative: Full Database URL (for production)
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# JWT Secret Keys
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here_change_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# OAuth Configuration (Optional)
# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Facebook OAuth
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret
FACEBOOK_CALLBACK_URL=http://localhost:5000/api/auth/facebook/callback

# Email Service (for OTP and notifications)
# Using Gmail SMTP as example
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@budgee.com
SMTP_FROM_NAME=Budgee Finance

# File Upload (Optional - for receipts)
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880

# ================================================
# Production Environment Variables
# ================================================
# Make sure to update these for production:
# 1. Use strong, randomly generated JWT secrets
# 2. Use DATABASE_URL from your hosting provider
# 3. Set NODE_ENV=production
# 4. Update CORS_ORIGIN to your frontend domain
# 5. Enable SSL for database connection
# ================================================
